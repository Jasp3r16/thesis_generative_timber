# Keeps the logs/ directory in the repo so SLURM's --output=logs/... can open
# its file. slurmstepd creates the output file but NOT missing parent dirs, so
# this directory must exist before sbatch runs. Do not delete.
